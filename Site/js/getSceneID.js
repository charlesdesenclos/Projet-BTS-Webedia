/* function displaySceneId(sceneId) {
    // Affiche l'ID de la scène sur le site
    var sceneIdElement = document.getElementById('scene-id');
    sceneIdElement.textContent = "ID de la scène : " + sceneId;
} */

function displaySceneId(id) {
    sendSceneId(id);
    console.log(id);
}